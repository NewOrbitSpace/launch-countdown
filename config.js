// Launch configuration — edit this file to change the countdown target.
// PLACEHOLDER DATE: update before going live.
const LAUNCH_CONFIG = {
  title: "Launch",                    // Heading shown above the countdown
  subtitle: "T-minus",                // Small label above the digits
  // ISO 8601 format with timezone offset (e.g. "2026-12-01T09:00:00+00:00")
  launchDate: "2026-12-31T12:00:00+00:00",
  // Message shown once the countdown reaches zero
  launchedMessage: "We have liftoff!",
};
